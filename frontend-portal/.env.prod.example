# ============================================
# iDream Frontend Portal - Production Environment
# ============================================
# Production environment configuration
# Copy this file to .env.prod and fill in your values
# NEVER commit .env.prod file to version control

# ============================================
# API Configuration
# ============================================
# Backend API URL for production
# Replace with your production API URL
REACT_APP_API_URL=https://api.idreamegypt.com/api

# ============================================
# Admin Portal URL
# ============================================
# URL of the admin portal (used for enterprise portal links)
# Replace with your production admin portal URL
REACT_APP_ADMIN_PORTAL_URL=https://admin.idreamegypt.com

# ============================================
# Production Notes
# ============================================
# 1. REACT_APP_API_URL: Set to your production backend API URL
# 2. REACT_APP_ADMIN_PORTAL_URL: Set to your production admin portal URL
# 3. Build: Run 'npm run build' to create production build
# 4. Serving: Use nginx, Apache, or a CDN to serve the build folder
# 5. Environment variables must start with REACT_APP_ to be accessible in React
# 6. Environment variables are embedded at build time, not runtime
# 7. After changing .env.prod, rebuild the application

