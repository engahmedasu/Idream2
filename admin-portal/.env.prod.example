# ============================================
# iDream Admin Portal - Production Environment
# ============================================
# Production environment configuration
# Copy this file to .env.prod and fill in your values
# NEVER commit .env.prod file to version control

# ============================================
# API Configuration
# ============================================
# Backend API URL for production
# Replace with your production API URL
REACT_APP_API_URL=https://api.idreamegypt.com/api

# ============================================
# Production Notes
# ============================================
# 1. REACT_APP_API_URL: Set to your production backend API URL
# 2. Build: Run 'npm run build' to create production build
# 3. Serving: Use nginx, Apache, or a CDN to serve the build folder
# 4. Environment variables must start with REACT_APP_ to be accessible in React
# 5. Environment variables are embedded at build time, not runtime
# 6. After changing .env.prod, rebuild the application

