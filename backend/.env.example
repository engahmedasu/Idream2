# ============================================
# iDream Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your production values
# NEVER commit .env file to version control

# ============================================
# Node Environment
# ============================================
# Options: development, production, test
NODE_ENV=production

# ============================================
# Server Configuration
# ============================================
# Port on which the backend server will run
PORT=5000

# ============================================
# Database Configuration
# ============================================
# MongoDB connection URI
# Format: mongodb://[username:password@]host[:port][/database][?options]
# For MongoDB Atlas (cloud): mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# For local MongoDB: mongodb://localhost:27017/idream
MONGODB_URI=mongodb://localhost:27017/idream

# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT token signing and verification
# IMPORTANT: Use a strong, random string in production (minimum 32 characters)
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# JWT token expiration time
# Format: number + unit (e.g., 7d = 7 days, 24h = 24 hours, 3600 = 3600 seconds)
JWT_EXPIRE=7d

# ============================================
# Email Configuration (SMTP)
# ============================================
# Email service host (e.g., smtp.gmail.com, smtp.mailgun.org, smtp.sendgrid.net)
EMAIL_HOST=smtp.gmail.com

# Email service port (587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Email account username (usually the full email address)
# For Gmail: Use your full Gmail address (e.g., yourname@gmail.com)
EMAIL_USER=your-email@gmail.com

# Email account password
# For Gmail: Use an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
# Make sure 2-Factor Authentication is enabled on your Gmail account
EMAIL_PASS=your-email-app-password

# ============================================
# Production Notes
# ============================================
# 1. Database: Use MongoDB Atlas or a managed MongoDB service for production
# 2. JWT_SECRET: MUST be changed to a strong random string in production
# 3. Email: Configure SMTP settings according to your email provider
# 4. CORS: If deploying frontend separately, update CORS settings in server.js
# 5. File Uploads: Ensure uploads/ directory has proper permissions
# 6. HTTPS: Use reverse proxy (nginx/Apache) with SSL certificate for production

