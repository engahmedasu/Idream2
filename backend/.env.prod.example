# ============================================
# iDream Backend - Production Environment
# ============================================
# Production environment configuration
# Copy this file to .env.prod and fill in your values
# NEVER commit .env.prod file to version control
 
# ============================================
# Node Environment
# ============================================
NODE_ENV=production
 
# ============================================
# Server Configuration
# ============================================
# Port on which the backend server will run
# In production, this is typically handled by a reverse proxy (nginx)
PORT=5000
 
# ============================================
# Database Configuration
# ============================================
# MongoDB connection URI for production
# MUST use MongoDB Atlas or a managed MongoDB service
# Format: mongodb+srv://username:password@cluster.mongodb.net/idream?retryWrites=true&w=majority
#MONGODB_URI=mongodb://idream:AppStrongPassword123@127.0.0.1:27017/idream?authSource=idream&retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://idream_db_user:aihKqt50kYMGiwtr@psteg.qmqc74r.mongodb.net/idream?appName=psteg
 
 
# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT token signing and verification
# CRITICAL: MUST be a strong, random string (minimum 32 characters)
# Generate secure key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=31b179b84e1a62b6c53b0d8b634225e6b7b5568bfd82e9424a7797c2d1a8a1a1
 
# JWT token expiration time
# Format: number + unit (e.g., 7d = 7 days, 24h = 24 hours, 3600 = 3600 seconds)
JWT_EXPIRE=7d
 
# ============================================
# Email Configuration (SMTP)
# ============================================
# Email service host (e.g., smtp.gmail.com, smtp.mailgun.org, smtp.sendgrid.net)
EMAIL_HOST=smtppro.zoho.com
 
# Email service port (587 for TLS, 465 for SSL)
EMAIL_PORT=587
 
# Email account username (usually the full email address)
# For Gmail: Use your full Gmail address (e.g., yourname@gmail.com)
# For production, consider using a dedicated email service
EMAIL_USER=no.reply@idreamegypt.com

# Email account password
# For Gmail: Use an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
# Make sure 2-Factor Authentication is enabled on your Gmail account
# For production, consider using a dedicated email service (SendGrid, Mailgun, etc.)
EMAIL_PASS=iDreamEgypt@4312
 
ALLOWED_ORIGINS=https://mall.idreamegypt.com,https://idreamegypt.com,https://admin.idreamegypt.com,api.idreamegypt.…
 
# ============================================
# Production Notes
# ============================================
# 1. Database: MUST use MongoDB Atlas or managed MongoDB service
# 2. JWT_SECRET: MUST be changed to a strong random string
# 3. Email: Configure 
SMTP settings according to your email provider
# 4. CORS: Update CORS settings in server.js for production domains
# 5. File Uploads: Ensure uploads/ directory has proper permissions
# 6. HTTPS: Use reverse proxy (nginx/Apache) with SSL certificate
# 7. Monitoring: Set up logging and monitoring for production
# 8. Backup: Configure automated database backups