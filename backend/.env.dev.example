# ============================================
# iDream Backend - Development Environment
# ============================================
# Development environment configuration
# Copy this file to .env.dev and fill in your values
# NEVER commit .env.dev file to version control

# ============================================
# Node Environment
# ============================================
NODE_ENV=development

# ============================================
# Server Configuration
# ============================================
# Port on which the backend server will run
PORT=5000

# ============================================
# Database Configuration
# ============================================
# MongoDB connection URI for development
# Local MongoDB: mongodb://localhost:27017/idream
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/idream-dev?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/idream

# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT token signing and verification
# For development, you can use a simple key (but still keep it secure)
# For production, use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=$2b$10$bytriQoeY5XsnVSi4jbNEe0/DPa1iLON9GuG./2n4Po/6jG6PvFla

# JWT token expiration time
# Format: number + unit (e.g., 7d = 7 days, 24h = 24 hours, 3600 = 3600 seconds)
JWT_EXPIRE=7d


# ============================================
# Email Configuration (SMTP)
# ============================================
# Email service host (e.g., smtp.gmail.com, smtp.mailgun.org, smtp.sendgrid.net)
EMAIL_HOST=smtppro.zoho.com

# Email service port (587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Email account username (usually the full email address)
# For Gmail: Use your full Gmail address (e.g., yourname@gmail.com)
# For production, consider using a dedicated email service
EMAIL_USER=no.reply@idreamegypt.com

# Email account password
# For Gmail: Use an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
# Make sure 2-Factor Authentication is enabled on your Gmail account
# For production, consider using a dedicated email service (SendGrid, Mailgun, etc.)
EMAIL_PASS=iDreamEgypt@312

# ============================================
# Development Notes
# ============================================
# 1. Database: Local MongoDB or development MongoDB Atlas cluster
# 2. JWT_SECRET: Can use a simpler key for development (but still keep it secure)
# 3. Email: Configure SMTP settings for development testing
# 4. CORS: Should allow localhost origins for development
# 5. File Uploads: Ensure uploads/ directory has proper permissions

